import HomeController from "controllers/home.controller";

/**
 * The array below is useful for registering your controllers and routes!
 * @type {import('@router').MydControllerConstructor[]}
 */
export default [HomeController];
